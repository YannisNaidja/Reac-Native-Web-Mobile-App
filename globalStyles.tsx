import { StyleSheet } from 'react-native';

/** global styles sheet for the app
 * use: import styles from './globaStyles' 
 * adapt import path if needed
 */
export default StyleSheet.create({
  container: {
    flex: 1
  },
  welcome: {
    fontSize: 20
  }
});